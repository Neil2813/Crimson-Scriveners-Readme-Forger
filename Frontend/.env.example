# ── Development only (Vite dev server uses the proxy, so this is ignored) ────
VITE_API_BASE_URL=http://localhost:8000

# ── Production (Vercel) ───────────────────────────────────────────────────────
# Set this in: Vercel → Project Settings → Environment Variables
# Value: your Render backend URL, e.g. https://csrf-api.onrender.com
# VITE_API_BASE_URL=https://csrf-api.onrender.com
